# Frontend
# Google Gemini API key used by client-side metadata generator. Avoid exposing production keys in client builds.
VITE_GEMINI_API_KEY=AQ.Ab8RN6IpFWx9-TvWyYXa6wtMpoejII3LaYINBazpOz-Xn7zJYQ
# Backend URL exposed to the client (Cloud Run backend URL or local docker-compose URL)
VITE_BACKEND_URL=https://streamhub-pro-rnowwd4svq-uw.a.run.app

# Backend - HTTP/WebSocket server
PORT=3000
NODE_ENV=development

# PostgreSQL connection (choose DATABASE_URL for TCP or Cloud SQL socket settings below)
DATABASE_URL=postgresql://streamhub:streamhub@/streamhub?host=/cloudsql/wtp-apps:us-east1:streamhub-db-4beb
DB_USER=streamhub
DB_PASS=streamhub
DB_NAME=streamhub
# Cloud SQL instance connection name, e.g. project:region:instance
INSTANCE_CONNECTION_NAME=wtp-apps:us-east1:streamhub-db-4beb

# Redis (optional)
REDIS_HOST=localhost
REDIS_PORT=6379

# Streaming
# Set to true to enable RTMP server in Node Media Server
ENABLE_RTMP=false
